# Optional: only needed for private/gated repos.
HF_TOKEN=

# Server-token mode is disabled unless this is set to 1.
ALLOW_SERVER_TOKEN=0

# Optional safety scope for public deployments, comma-separated.
TOKEN_ALLOWED_OWNERS=
